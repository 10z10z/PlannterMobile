# Commits that changed formatting and nothing else. `git blame` skips them, so
# a line's history points at whoever wrote it rather than at the reformat.
#
# GitHub reads this file automatically. Locally:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# Format the codebase with Prettier
f702e70b32404470f8b29bfb01f2222b52c6e8b0
